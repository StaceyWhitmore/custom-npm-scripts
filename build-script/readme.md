Just run `npm run build` to run the entire npm script which:
* cleans out the `dist` directory and replaces it with a fresh empty one.
* then, runs `webpack` again to rebuild a fresh `dist` directory 
